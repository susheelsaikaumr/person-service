management:
  endpoints:
    enabled-by-default: true
  info:
    enabled: true
server:
  port: 5000

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic
